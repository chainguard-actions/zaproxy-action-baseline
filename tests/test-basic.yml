name: Test ZAP Baseline Scan - Basic
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        continue-on-error: true
        with:
          target: 'https://www.example.com'
          docker_name: 'ghcr.io/zaproxy/zaproxy:stable'
          allow_issue_writing: 'false'
          fail_action: 'false'
          artifact_name: 'zap_scan_basic'

      - name: Assert action ran (success or graceful failure)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "success" ]; then
            echo "PASS: ZAP baseline scan completed successfully"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action ran but failed (may be expected in constrained environments)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
